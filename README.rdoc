A brief introduction : Ruby backend+Ember.js

To render the application in local host visit localhost:3000/#/stories

The different routes are nested inside one another, different stories can be added or deleted and a hook has been run to ensure the data doesn't add the title until you finish typing.
